import { InputTemperatureDirective } from './input-temperature.directive';

describe('InputTemperatureDirective', () => {
  it('should create an instance', () => {
    const directive = new InputTemperatureDirective();
    expect(directive).toBeTruthy();
  });
});
