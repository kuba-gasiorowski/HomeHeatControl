import { InputTimeDirective } from './input-time.directive';

describe('InputTimeDirective', () => {
  it('should create an instance', () => {
    const directive = new InputTimeDirective();
    expect(directive).toBeTruthy();
  });
});
